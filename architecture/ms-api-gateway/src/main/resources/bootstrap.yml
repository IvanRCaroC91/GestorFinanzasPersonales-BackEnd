spring:
  application:
    name: ms-api-gateway
  cloud:
    config:
      uri: ${CONFIG_SERVER_URI:http://localhost:8890}
      username: ${CONFIG_SERVER_USERNAME:admin}
      password: ${CONFIG_SERVER_PASSWORD:admin123}
      label: main
      fail-fast: true
      retry:
        initial-interval: 1000
        max-attempts: 6
        max-interval: 2000
        multiplier: 1.5
